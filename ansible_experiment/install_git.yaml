- name: Install Git on all servers
  hosts: servers
  become: true
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
      when: ansible_os_family == "Debian"

    - name: Install Git
      package:
        name: git
        state: present
